## V1 Augmented Shenanigans

```js
var BasicElement = function (_HTMLElement) {
  _inherits(BasicElement, _HTMLElement);

  function BasicElement() {
    _classCallCheck(this, BasicElement);

    return _possibleConstructorReturn(this,
      (BasicElement.__proto__ ||
      Object.getPrototypeOf(BasicElement)
    ).apply(this, arguments));
  }

  return BasicElement;
}(HTMLElement);
```